#include "main.h"

/**
 *_abs - Check out description
 * @n: Is an input parameter
 * Description: Give out the absolute value of a number
 * Return: Absolut value of number n
 */
int _abs(int n)
{
if (n >= 0)
return (n);
else
return (n * -1);
}
