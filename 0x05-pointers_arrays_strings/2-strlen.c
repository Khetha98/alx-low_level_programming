#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - this function takes in a character and
 * give out its lenght
 * @s: input parameter of char type
 * Return: integer
 **/

int _strlen(char *s)
{

return (strlen(s));
}
